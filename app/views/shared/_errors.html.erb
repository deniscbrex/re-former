 <div id="error_explanation">
  <% if @user.errors.any? %>
    <h2>
      <%= pluralize(@user.errors.count, "error") %> prevented this from saving
    </h2>
    <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg%></li>
      <% end %>
    </ul>
  <% end %>
</div>